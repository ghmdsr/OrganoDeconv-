# 로컬 개발: 비우면 /api → Vite proxy → localhost:5188
# VITE_API_URL=
#
# Vercel 배포: Environment Variables에 동일하게 설정 권장 (Key: VITE_API_URL)
# Value: https://api-organodeconv.genome-intelligence-lab.org  (/api 접미사 없음)
# VITE_API_BASE_URL 은 사용하지 않음. 미설정 시 빌드 코드의 PROD 기본값 사용.
